import type {Cpu,DealConfig,Filters} from "@/data/types";import {fmtMoney} from "@/lib/metrics";import {Panel,Title} from "./ui";
export function NoExactMatch({items,deal,onRelax}:{items:{cpu:Cpu;failures:string[]}[];deal:DealConfig;onRelax:()=>void}){if(!items.length)return <Panel><Title color="#ff5252">No Platform-Compatible CPU</Title><div style={{padding:12,fontSize:12}}>No CPU in the catalog supports this server/socket configuration. Check the selected server and CPUs per server.</div></Panel>;return <Panel><Title color="#ffb347">No Exact Match — Closest Compatible Options</Title><div style={{padding:10}}><div style={{fontSize:11,color:"#cbd5e1",marginBottom:7}}>These CPUs remain valid for the selected platform and {deal.socketsPerServer} CPU{deal.socketsPerServer>1?"s":""}/server, but miss one or more performance/commercial filters.</div>{items.map(({cpu,failures})=><div key={cpu.sku} style={{display:"grid",gridTemplateColumns:"110px 1fr 90px",gap:8,padding:"7px 0",borderTop:"1px solid rgba(97,145,175,.2)",fontSize:11}}><b style={{color:"#fff"}}>Xeon {cpu.sku}</b><span style={{color:"#ffca80"}}>Misses: {failures.join(", ")}</span><span style={{textAlign:"right"}}>{fmtMoney(cpu.costUsd)}</span></div>)}<button onClick={onRelax} style={{width:"100%",marginTop:8,fontWeight:800}}>Relax Performance Filters</button></div></Panel>}